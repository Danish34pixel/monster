// Dev script moved to scripts/dev/generateResetToken.js
// This top-level script has been intentionally replaced to avoid accidental use
// in production. Use the script under scripts/dev if you need to generate a reset token.

console.log('This script was moved to scripts/dev/generateResetToken.js');
console.log('Run: node scripts/dev/generateResetToken.js from the project root (Medi-trap-backend)');
